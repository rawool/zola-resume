# The URL the site will be built for
title = "Jonathan Vercoutre - Senior Site Reliability Engineer"                                                                     # Your site name
description = "Resume page of Jonathan Vercoutre, Senior Site Reliability Engineer, in Lille, France, with 18 years of experience." # Shown in meta and hero
base_url = "https://jonathan.vercout.re"

# Use base_url present below if deploying using GitHub Pages
# base_url = "https://yourusername.github.io/BelResume"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

[extra]
# Put all your custom variables here
